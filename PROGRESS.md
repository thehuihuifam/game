# PROGRESS.md — 진행 원장

## 원장 (완료 Loop — 영구 보존, 삭제·번호 재배치 금지)

| Loop | PR | 상태 | 결과 (1~2줄 압축) |
|------|----|------|------------------|
| 1 | [#1](https://github.com/thehuihuifam/game/pull/1) | ✅ 완료 | 세션 부트스트랩. AGENTS.md·PROGRESS.md 생성 + `index.html`에 세로 고정·도형만·터치 카드 선택·효과 표시가 도는 최소 플레이 가능 프로토타입. |
| 1 | [Fix] [#1](https://github.com/thehuihuifam/game/pull/1) | ✅ 완료 | `MATERIALS.md` 소재 노트 추가 + AGENTS.md 세션 규칙에 "카드·이벤트·대사 소재는 MATERIALS.md 참조" 한 줄 추가. |
| 2 | [#2](https://github.com/thehuihuifam/game/pull/2) | ✅ 완료 | 큐 목표를 AGENTS.md `시스템 스펙`(스키마·전이표 T0~T5·무결성 규칙 5개)으로 승격 후 구현. `index.html`을 DATA/RULE/VIEW/CONTROLLER로 분리하고 자원·카드·팩(morning·school·home)·하루 사이클·문구를 데이터화. 죽음·승리도 효과 화면을 거치게 통일, 완주 화면 `undefined` 버그 수정. 밸런스 숫자는 Loop 1 그대로(패리티 검사 포함 311,653건·120판 fuzz 통과). |
| 3 | [#3](https://github.com/thehuihuifam/game/pull/3) | ✅ 완료 | 카드 7→20장(신규 13장) + `CARD.minDay`(2·3일차 게이트) 승격으로 3일 압박 곡선을 DATA만으로 구현. 전 카드 수치 재조정 → 무작위 완주 38.4%(죽음 3경로 모두 관측)·최선 플레이 79~100%·최악 0.2%, 하루 중복 0건. 검사 하네스 `tools/`(check·sim·dom-smoke) 추가. |
| 4 | [#4](https://github.com/thehuihuifam/game/pull/4) | ✅ 완료 | 선택 결과 CSS 피드백(200~250ms)·실제 변화 자원만 강조·reduce 0ms·작은 세로 화면 보완. check 23건·DOM 200판·시드 고정 2,000판/54,042상태 Loop 3 패리티 및 Chromium 전 카드·3개 뷰포트 검사 통과. |
| 5 | [#5](https://github.com/thehuihuifam/game/pull/5) | ✅ 완료 | 종료 화면에 생존/소진 사유, 진행 일차·선택 횟수·최종 자원 요약을 표시하고 승리 일차를 목표 일수로 보정. 마지막 효과→요약→재시작 흐름과 새 판 요약 초기화를 추가. check 23건·1,000판 sim 및 요약 불변식 1,000판 통과. DOM smoke는 jsdom 미설치, Chromium 모션 검사는 Playwright 미설치로 실행 불가. |
| 6 | PR_LINK_LOOP6 | ✅ 완료 | 단일 상태이상 「미룬 연락」(`state.status`) 승격: 연락을 미루는 선택(`parent`#1·`call`#1·`complain`#2)이 켜고, 켜진 동안 후속 카드 `reply`가 다음 학교 슬롯에서 **결정적으로** 뽑히며, 답하는 선택이 끈다. 상태 띠·선택지 미리보기 줄·효과 칩 추가, 무결성 규칙 7~8. check 30건·sim 2,000판×4(결정성 위반 0)·DOM 200판·Chromium 3개 뷰포트(상태 꺼짐·켜짐) 통과. 무작위 완주 38.7%(변화 없음), 근시안 greedy는 80→69%로 빚을 체감. |

## 다음 작업

1. Loop 5 — 완료. 종료 요약과 즉시 재시작 흐름 구현(원장 참조).
2. Loop 6 — 완료. 상태이상 「미룬 연락」 한 가지로 앞의 선택이 뒤의 카드를 결정적으로 바꾼다(원장·AGENTS.md `STATUSES` 참조).
3. **Loop 7 — 다음.** 요일 리듬 한 가지(수요일 4교시)로 3일이 서로 다르게 느껴지는 판 구조를 만든다.

## 진행 예정 Loop 스펙 (Loop 7 — 범위 계획)

**목표**: 한 판의 3일이 같은 하루의 반복이 아니라 요일에 따라 구조가 다르게 느껴지게 한다.

**범위**: MATERIALS.md 카테고리 1의 "수요일 전교생 4교시 하교" 소재를 픽션화한다. 일차마다 요일 이름을 상단에 보이고, 특정 일차(예: 2일차=수요일)의 슬롯 구성을 `DAY_CYCLE` 데이터만으로 바꾼다(예: 학교 슬롯 하나가 짧아지거나 회복 성격의 카드가 섞이는 팩으로 교체). 구체 스키마(일차별 슬롯 override, 요일 문구)와 숫자는 구현 시 승격한다.

**완료 조건**: 어느 일차에서도 슬롯 후보가 비지 않고 하루를 중복 없이 뽑을 수 있으며(규칙 5·6·8 확장), 요일 표시가 `render(state)`만으로 결정되고, check·sim·DOM(가능하면 Chromium) 검사를 통과한다. 무작위 완주율이 Loop 6 기준(약 39%)에서 크게 벗어나지 않는다.

**비목표**: 학기 단위 메타 진행·학기말 마감 이벤트(별도 Loop), 새 자원, 저장, 여러 요일 규칙의 조합.


## 큐 (미완료 Loop, AI가 유지·보충)

| Loop | 목표 |
|------|------|
| 7 | 요일 리듬(수요일 4교시)으로 3일의 구조를 다르게 만들기 ← **다음** |
| 8 | 날씨 변화에 따라 체육 수업 공간·선택지가 달라지는 분기 한 가지 추가 |
| 9 | 아이 돌봄 긴급상황이 낮 선택에서 저녁 카드로 이어지는 연쇄 한 가지 구현(Loop 6의 `needs` 후속 카드 재사용) |
| 10 | 선택형 카드 결과를 한눈에 비교할 수 있는 접근성·가독성 보완 |
| 11 | 학기말 마감(성적 산출·생기부)으로 마지막 날의 무게를 만드는 이벤트 한 가지 |

큐 점검: Loop 6 압축·`STATUSES` 승격 완료. 남은 Loop 4개(7~10)에 Loop 7 범위에서 뺀 "학기말 마감"을 11번으로 분리해 큐 5개 유지.
