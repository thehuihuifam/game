# PROGRESS.md — 진행 원장

## 원장 (완료 Loop — 영구 보존, 삭제·번호 재배치 금지)

| Loop | PR | 상태 | 결과 (1~2줄 압축) |
|------|----|------|------------------|
| 1 | [#1](https://github.com/thehuihuifam/game/pull/1) | ✅ 완료 | 세션 부트스트랩. AGENTS.md·PROGRESS.md 생성 + `index.html`에 세로 고정·도형만·터치 카드 선택·효과 표시가 도는 최소 플레이 가능 프로토타입. |
| 1 | [Fix] [#1](https://github.com/thehuihuifam/game/pull/1) | ✅ 완료 | `MATERIALS.md` 소재 노트 추가 + AGENTS.md 세션 규칙에 "카드·이벤트·대사 소재는 MATERIALS.md 참조" 한 줄 추가. |
| 2 | [#2](https://github.com/thehuihuifam/game/pull/2) | ✅ 완료 | 큐 목표를 AGENTS.md `시스템 스펙`(스키마·전이표 T0~T5·무결성 규칙 5개)으로 승격 후 구현. `index.html`을 DATA/RULE/VIEW/CONTROLLER로 분리하고 자원·카드·팩(morning·school·home)·하루 사이클·문구를 데이터화. 죽음·승리도 효과 화면을 거치게 통일, 완주 화면 `undefined` 버그 수정. 밸런스 숫자는 Loop 1 그대로(패리티 검사 포함 311,653건·120판 fuzz 통과). |

## 다음 작업

1. Loop 2 — 완료. 데이터 분리 + 상태 전이 리팩토링(PR #2).
2. **Loop 3 — 다음.** 스펙은 아래 "진행 예정 Loop 스펙" 참조. 시스템은 그대로고 카드 데이터만 늘리는 Loop.

## 진행 예정 Loop 스펙 (Loop 3 — 큐에서 승격)

> 스키마·상태 전이는 AGENTS.md `시스템 스펙`이 기준이다. 여기엔 이 Loop의 **범위·완료 조건**만 둔다.

**목표**: 카드 풀을 늘려 한 판(3일)이 시작·중반·결말의 굴곡을 갖게 한다. 새 시스템은 만들지 않고 **DATA만 추가**한다(스키마를 건드려야 하면 같은 PR에서 AGENTS.md 스펙도 갱신).

**범위**

- 팩별 카드 수: 아침 ≥3 · 학교 ≥5 · 집 ≥3 (총 ≥11장). 슬롯 후보가 늘어나 하루 안 중복 금지(`noRepeatInDay`) 안에서도 판이 달라진다.
- 소재는 `MATERIALS.md`에서 픽션화: 카1(세이프존 당번·비 예보·인솔 대기), 카2(악성 민원·미흡한 지원), 카3(메시지 한 줄·인식 격차), 카4(가방 두 개·어린이집 행사), 카6(업무분장표 빈칸·과밀 학급), 카7(서로의 오늘).
- 압박 곡선: 2·3일차에 회복 수단이 줄거나 비용이 커지는 카드를 섞는다. 목표는 **완주도 죽음도 실제로 일어나는** 난이도.
- 결말 문구는 기존 자원별 죽음 문구를 유지하고, 필요하면 `ENDINGS`에 문구만 추가한다.

**완료 조건**

1. 신규 카드 전부가 AGENTS.md "데이터 무결성 규칙 1~5"를 통과(하네스 자동 검사).
2. 무작위 플레이 시뮬레이션에서 죽음·완주가 **모두** 관측되고 완주율이 대략 20~60%에 든다(숫자는 이 Loop에서 최소치만 맞춘다).
3. 한 판에서 같은 카드가 하루 안에 반복되지 않는다.
4. 신규 카드는 전부 MATERIALS.md 소재의 픽션화이고 실제 인물·학교·지역·통계 수치가 나오지 않는다(MATERIALS.md "픽션화 규칙" 8개 준수).
5. 항상 플레이 가능: `python3 -m http.server`로 바로 돈다.

**비목표 (YAGNI)**: 상태이상·플래그·카드 연쇄(큐 6), 요일·학기 구조(큐 7), 애니메이션(Loop 4), 요약 화면(Loop 5), 새 자원 추가, 밸런스 전면 재설계.

## 큐 (미완료 Loop, AI가 유지·보충)

| Loop | 목표 |
|------|------|
| 3 | 카드 풀을 늘려 한 판의 시작·중반·결말(죽음/완주) 흐름 완성 ← **다음** (스펙 승격 완료) |
| 4 | 선택 결과 애니메이션·자원 게이지 표시 개선 |
| 5 | 죽음/승리 후 요약 화면과 "다시" 흐름 |
| 6 | 상태이상·플래그(예: 민원 누적, 온도차)로 앞의 선택이 뒤의 카드·수치에 영향을 주게 하기 |
| 7 | 요일·학기 리듬(수요일 4교시, 학기말 마감)으로 한 판의 구조 만들기 |
